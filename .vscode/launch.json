{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "start dev server",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run docs:dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}